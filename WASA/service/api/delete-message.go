import (
	//	"github.com/julienschmidt/httprouter"
		"net/http"
		"encoding/josn"
		"math/rand"
		"strconv"
	)
	
	
	func (rt *_roter) deleteMessage(w http.ResponseWriter, r *http.Request, ps httprouter.Params) {
		w.Header().Set("content(type", "application/json")
		
		
		
	
		
	
		json.NewEncoder(w).Encode(id)
	}